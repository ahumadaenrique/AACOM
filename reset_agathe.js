const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.user.findUnique({ where: { email: 'agathe.beaur@aacommx.com' } }); if (!user) { console.log('Usuario no encontrado'); return; } const progress = await prisma.agentDevelopmentProgress.upsert({ where: { userId: user.id }, update: { currentDayNumber: 1, status: 'IN_PROGRESS' }, create: { userId: user.id, currentDayNumber: 1, status: 'IN_PROGRESS' } }); console.log('Progreso reseteado con exito:', progress); } main().catch(e => console.error(e)).finally(() => prisma.$disconnect());
